# Dependencias ML, batch y desarrollo (Docker analytics + CI local)
# Version que reproduce el Modelo D publicado (models/prototype_model_d.pkl) de
# forma bit-identica, verificado 09-09-2026 -- ver docs/deploy.md.
# scikit-learn/joblib/rasterio/imbalanced-learn se heredan de requirements.txt
# desde el 09-09-2026 (son runtime, no solo training -- ver requirements.txt).
-r requirements.txt
scipy==1.18.1
xgboost==3.4.1
pytest==9.1.1
pytest-cov==7.1.0
flake8==7.3.0
black==26.5.1
locust==2.46.5
